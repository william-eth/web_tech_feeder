<!DOCTYPE html>
<html lang="zh-Hant">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>每週技術新知摘要</title>
  <style>
    body, table, td, p, a, li { -webkit-text-size-adjust: 100%; -ms-text-size-adjust: 100%; }
    body { margin: 0; padding: 0; width: 100% !important; }

    body {
      font-family: ui-sans-serif, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      font-size: 14px;
      line-height: 1.6;
      color: #334155;
      background-color: #f1f5f9;
    }

    .wrapper {
      max-width: 640px;
      margin: 0 auto;
      padding: 16px;
    }

    /* Header - dark technical style */
    .header {
      background: #0f172a;
      color: #e2e8f0;
      padding: 24px 24px;
      border-radius: 6px 6px 0 0;
    }
    .header h1 {
      margin: 0 0 6px 0;
      font-size: 18px;
      font-weight: 600;
      font-family: ui-monospace, 'SF Mono', 'Fira Code', 'Cascadia Code', monospace;
      letter-spacing: 0.5px;
    }
    .header .date-range {
      font-size: 12px;
      color: #94a3b8;
      margin: 0;
    }
    .header .item-count {
      display: inline-block;
      font-size: 12px;
      font-family: ui-monospace, monospace;
      color: #cbd5e1;
      background: #1e293b;
      padding: 4px 12px;
      border-radius: 4px;
      margin-top: 10px;
      font-weight: 500;
    }

    /* Content */
    .content {
      background: #ffffff;
      padding: 0;
      border-radius: 0 0 6px 6px;
      border: 1px solid #e2e8f0;
      border-top: none;
    }

    /* Section */
    .section {
      padding: 20px 24px;
      border-bottom: 1px solid #e2e8f0;
    }
    .section:last-child { border-bottom: none; }

    .section-header {
      margin-bottom: 16px;
      display: flex;
      align-items: baseline;
      gap: 8px;
    }
    .section-tag {
      font-size: 11px;
      font-family: ui-monospace, monospace;
      font-weight: 600;
      padding: 3px 8px;
      border-radius: 4px;
      letter-spacing: 0.5px;
    }
    .section-frontend .section-tag { background: #dbeafe; color: #1d4ed8; }
    .section-backend .section-tag { background: #fee2e2; color: #b91c1c; }
    .section-devops .section-tag { background: #dcfce7; color: #15803d; }

    .section-title {
      font-size: 15px;
      font-weight: 600;
      color: #0f172a;
      margin: 0;
    }
    .section-count {
      font-size: 12px;
      color: #94a3b8;
      font-family: ui-monospace, monospace;
    }

    .subsection-title {
      font-size: 12px;
      font-weight: 600;
      color: #64748b;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin: 14px 0 8px 0;
    }
    .subsection-title:first-of-type { margin-top: 0; }

    /* Item - document/card style */
    .item {
      margin-bottom: 12px;
      padding: 14px 16px;
      border-radius: 4px;
      background: #f8fafc;
      border: 1px solid #e2e8f0;
      border-left: 3px solid #94a3b8;
    }
    .item:last-child { margin-bottom: 0; }

    .item-critical { border-left-color: #dc2626; background: #fef2f2; }
    .item-high { border-left-color: #ea580c; background: #fff7ed; }
    .item-medium { border-left-color: #2563eb; background: #eff6ff; }
    .item-low { border-left-color: #94a3b8; }

    .item-title {
      font-size: 14px;
      font-weight: 600;
      color: #0f172a;
      margin: 0 0 6px 0;
      line-height: 1.4;
    }
    .item-title a {
      color: #0369a1;
      text-decoration: none;
    }
    .item-title a:hover { text-decoration: underline; }

    .importance-badge {
      display: inline-block;
      font-size: 10px;
      font-family: ui-monospace, monospace;
      font-weight: 600;
      padding: 2px 6px;
      border-radius: 3px;
      text-transform: uppercase;
      letter-spacing: 0.3px;
      vertical-align: middle;
      margin-left: 8px;
    }
    .badge-critical { background: #fecaca; color: #b91c1c; }
    .badge-high { background: #fed7aa; color: #c2410c; }
    .badge-medium { background: #bfdbfe; color: #1d4ed8; }
    .badge-low { background: #cbd5e1; color: #475569; }

    .framework-badge {
      display: inline-block;
      font-size: 11px;
      font-family: ui-monospace, monospace;
      font-weight: 500;
      padding: 2px 8px;
      border-radius: 3px;
      background: #e2e8f0;
      color: #475569;
      margin-right: 8px;
      vertical-align: middle;
    }

    .item-summary {
      font-size: 13px;
      color: #475569;
      margin: 0 0 8px 0;
      line-height: 1.55;
      word-break: break-word;
    }
    .summary-part { margin-bottom: 8px; }
    .summary-part:last-child { margin-bottom: 0; }
    .summary-part-body { word-break: break-word; }
    .summary-part-label {
      font-size: 12px;
      font-weight: 600;
      color: #64748b;
      margin-bottom: 3px;
    }
    .summary-code {
      display: block;
      font-family: ui-monospace, 'SF Mono', 'Fira Code', monospace;
      font-size: 12px;
      background: #f1f5f9;
      border: 1px solid #e2e8f0;
      border-radius: 4px;
      padding: 10px 12px;
      margin: 6px 0;
      white-space: pre-wrap;
      word-break: break-word;
      overflow-x: auto;
      max-width: 100%;
    }
    .summary-inline-code {
      font-family: ui-monospace, 'SF Mono', 'Fira Code', monospace;
      font-size: 0.9em;
      background: #e2e8f0;
      color: #334155;
      padding: 2px 6px;
      border-radius: 3px;
    }

    .item-source {
      font-size: 11px;
      color: #94a3b8;
      margin: 0;
    }
    .item-source a {
      color: #64748b;
      text-decoration: none;
    }

    .empty-state {
      text-align: center;
      padding: 24px;
      color: #94a3b8;
      font-size: 13px;
    }

    /* Footer */
    .footer {
      text-align: center;
      padding: 16px;
      font-size: 11px;
      color: #94a3b8;
    }
    .footer a { color: #64748b; }

    @media only screen and (max-width: 600px) {
      .wrapper { padding: 12px; }
      .header { padding: 20px; }
      .section { padding: 16px; }
      .item { padding: 12px; }
    }
  </style>
</head>
<body>
  <div class="wrapper">
    <div class="header">
      <h1>每週技術新知摘要</h1>
      <p class="date-range"><%= date_range %></p>
      <span class="item-count">共 <%= total_items %> 則</span>
    </div>

    <div class="content">
      <% categories = [
        { key: :frontend, css: "section-frontend", tag: "FE", fallback_title: "前端技術動態" },
        { key: :backend, css: "section-backend", tag: "BE", fallback_title: "後端技術動態" },
        { key: :devops, css: "section-devops", tag: "OPS", fallback_title: "DevOps 相關資訊" }
      ] %>

      <% categories.each do |cat| %>
        <% section = digest_data[cat[:key]] %>
        <% next unless section %>
        <% release_items = section[:release_items] || [] %>
        <% other_items = section[:other_items] || [] %>
        <% item_count = release_items.size + other_items.size %>
        <% item_count = (section[:items]&.size || 0) if item_count.zero? %>
        <div class="section <%= cat[:css] %>">
          <div class="section-header">
            <span class="section-tag"><%= cat[:tag] %></span>
            <h2 class="section-title"><%= section[:section_title] || cat[:fallback_title] %></h2>
            <span class="section-count">· <%= item_count %></span>
          </div>

          <% if item_count.zero? %>
            <div class="empty-state">本週暫無相關更新</div>
          <% else %>
            <% [[ "版本釋出", release_items, true ], [ "其他動態", other_items, release_items.any? ]].each do |subsection_title, items, show_title| %>
              <% next if items.empty? %>
              <% if show_title %><h3 class="subsection-title"><%= subsection_title %></h3><% end %>
              <% items.each do |item| %>
                <% importance = (item[:importance] || "medium").downcase %>
                <% fw = item[:framework_or_package].to_s.strip %>
                <% show_fw_badge = !fw.empty? && subsection_title == "其他動態" %>
                <div class="item item-<%= importance %>">
                  <p class="item-title">
                    <% if show_fw_badge %><span class="framework-badge"><%= fw %></span><% end %>
                    <% if item[:source_url] && !item[:source_url].to_s.empty? %>
                      <a href="<%= item[:source_url] %>"><%= truncate_text(item[:title].to_s, 120) %></a>
                    <% else %>
                      <%= truncate_text(item[:title].to_s, 120) %>
                    <% end %>
                    <span class="importance-badge badge-<%= importance %>"><%= importance_label(importance) %></span>
                  </p>
                  <div class="item-summary">
                    <% summary_parts(item[:summary]).each do |label, content| %>
                      <% next if content.to_s.strip.empty? %>
                      <div class="summary-part">
                        <% if label %>
                          <div class="summary-part-label"><%= label %></div>
                          <div class="summary-part-body"><%= format_summary_content(truncate_text(content.to_s, 1200)) %></div>
                        <% else %>
                          <div class="summary-part-body"><%= format_summary_content(truncate_text(content.to_s, 1200)) %></div>
                        <% end %>
                      </div>
                    <% end %>
                  </div>
                  <p class="item-source">
                    <% if item[:source_url] && !item[:source_url].to_s.empty? %>
                      <a href="<%= item[:source_url] %>"><%= item[:source_name] || shorten_url(item[:source_url].to_s) %></a>
                    <% else %>
                      <%= item[:source_name] || "Unknown" %>
                    <% end %>
                  </p>
                </div>
              <% end %>
            <% end %>
          <% end %>
        </div>
      <% end %>
    </div>

    <div class="footer">
      <a href="https://github.com/william-eth/web_tech_feeder">Web Tech Feeder</a> · AI-powered digest
    </div>
  </div>
</body>
</html>
